/*Create a code that contains:
 * A class to represent a checking account containing: 
 * number, balance, overdraft and limit. 
 * Create methods to carry out a withdrawal 
 * (verifying if the customer can execute a withdrawal), deposit money, 
 * check the balance and verify if the customer is using an overdraft or not.
 */

package methodsOOP;

public class CheckingAccount {
	String number;
	String branch;
	Boolean overdraft;
	double overdraftLimit;
	double balance;
	
	
	
}
