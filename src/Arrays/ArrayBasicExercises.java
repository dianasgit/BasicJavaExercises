package Arrays;
import java.util.Scanner;
public class ArrayBasicExercises {

	public static void main(String[] args) {
		
		Scanner scan = new Scanner(System.in);

		/* Array também chamados de vatores = é uma lista ordenada de dados... em java para criar um  array já se delimita um tamanho pra ele e a primeira [cada posição é chamada de index] é a posição  [0].
		
para declarar um array é assim:
		*tipo [] nome = new tipo[tamanho em numero];
		
		*os colchetes podem vir após o nome, porém é boa prática colocar após o tipo e antes do nome.
		//por exemplo, para imprimir uma valor expecífico da lista pode-se chamar ele pela posição: exemplo nome[numero] dentro do SOPLN
		
		*para saber o tamanho de um array pode-se usar a propriedade  nome.lenght como var na hora de imprimir porém isso vai imprimir quantas posições existem no total ... e não necessariamente quantas estão disponiveis ou ocupadas.
		
		*para obter TODAS as informações disponiveis no array, seja ela ocupada ou não deve-se utilizar o loop for... se tentar imprimir o nome da array direto como se fosse uma var o que vai aparecer é o endereço de memória apenas.
		
*exemplo
 * 
 * for (int i=0 ; i<nome.lenght ; i++){
 * 	System.out.println("o valor do dia" +(i+1) + " = " + nome[i]);
 * 
 * nesse exemplo supondo que cada espaço armazena um dia por isso de usou i+1 pois a contagem de index começa em zero. isso vai imprimir Nx a frase conforme N tamanhã do array
 */
		
//#############################################################
/*		
		// crie um vertor A com 5 elementos inteiros. Construir um vetor B de mesmo tipo e tamanho e com os "mesmo" elementos do vator A.
		
		System.out.println("Inform 5 diferente values, any type:");
		String [] valueA = new String[5];
		String [] valueB = new String[valueA.length]; //usar o length sempre que se refere ao tamanho é uma boa pratica considerando que pose ser que com o tempo o vetor A mude ...
		
		for (int i =0  ;  i<valueA.length  ; i++) {
			
			System.out.println("Digite o valor da posição: "+ i);
			valueA[i] = scan.next();
			valueB[i] = valueA[i];
		}
		
		System.out.print("\nVetor A = ");
		for (int i =0 ; i<valueA.length ; i++ ) {
			System.out.print(valueA[i] + " ");
		}
		
		System.out.print("\nVetor B = ");
		for (int i =0 ; i<valueA.length ; i++ ) {
			System.out.print(valueB[i] + " ");
		}
*/		
		
		
//#############################################################
		// write a code that receives a list A with 8 numeric elements and a list B with the same elements multiplied by 2. print both at the end. 
		
		
		
		
		
		
		
		
		
		
		
//------------------------		
	}
}
